# FocusTrack

A local-first productivity tracker that monitors desktop app and website usage with privacy-focused activity analytics.

## Project Structure

```text
focus-track/
├── desktop-app/
└── chrome-extension/
```

## Current Scope

The current implementation work is focused on `desktop-app/`, which contains the Electron + React desktop application.

